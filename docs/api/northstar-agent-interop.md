# northstar-agent-interop - API reference

<!-- GENERATED by tests/docbuild.py - do not edit; run `python3 tests/docbuild.py build` -->

Docstring-generated reference over the component's public modules (``ast``-extracted; nothing is imported, so this builds offline with the standard library). The README is the quick start and integration guide:

- [README (quick start)](../../components/northstar-agent-interop/README.md)
- [Concepts](../../docs/concepts/governance.md) and [guides](../../docs/guides/governed-run-cookbook.md)

## Modules

### `interop_contract`

Source: `components/northstar-agent-interop/interop_contract.py`

Strict backend-neutral contracts for Northstar Agent handoffs.

#### `AgentProfile`

- `from_dict(value: Any)`
- `to_dict()`
- `canonical_json()`
#### `AgentRegistry`

- `register(profile: AgentProfile)`
- `get(agent_id: str)`
#### `AgentAttestation`

- `from_dict(value: Any)`
- `to_dict()`
- `canonical_json()`
#### `HandoffRequest`

- `from_dict(value: Any)`
- `to_dict()`
- `canonical_json()`
#### `HandoffGrant`

- `from_dict(value: Any)`
- `to_dict()`
- `canonical_json()`
#### `assert_attestation_identity(attestation: AgentAttestation, request: HandoffRequest)`

#### `assert_grant_identity(request: HandoffRequest, grant: HandoffGrant)`

### `handoff`

Source: `components/northstar-agent-interop/handoff.py`

Signed Agent attestations and narrowed handoff grants.

#### `HandoffValidation`

#### `sign_attestation(attestation: AgentAttestation, secret: bytes)`

#### `verify_attestation(token: str, secret: bytes, *, now: int)`

#### `sign_handoff_grant(grant: HandoffGrant, secret: bytes)`

#### `verify_handoff_grant(token: str, secret: bytes, *, now: int)`

#### `authorize_handoff(request: HandoffRequest, *, parent_authorization: AuthorizationValidation | None=None, parent_handoff: HandoffValidation | None=None, source_attestation: HandoffValidation, registry: AgentRegistry, now: int, current_policy_revision: str | None=None, secret: bytes, grant_ttl_seconds: int=300)`

### `interop_adapter`

Source: `components/northstar-agent-interop/interop_adapter.py`

Backend-neutral adapter contract for future Agent engines.

#### `ContextEnvelope`

- `from_dict(value: Any)`
- `to_dict()`
#### `AdapterExecution`

- `to_dict()`
#### `AdapterReceipt`

- `from_dict(value: Any)`
- `to_dict()`
#### `BackendAdapter`

Run one target backend behind a verified, target-bound handoff.

- `execute(handoff_token: str, context: ContextEnvelope, *, handoff_secret: bytes, current_policy_revision: str, now: int)`
#### `AdapterExecutionContext`

- `to_dict()`
### `canary`

Source: `components/northstar-agent-interop/canary.py`

Local-only multi-backend canary for the Northstar interop boundary.

#### `CanaryOutcome`

#### `run_canary(root: str | Path, *, replay_codex: bool=False, tamper_result: bool=False, review_verdict: str='verified', current_policy_revision: str=_POLICY_REVISION)`

### `process_adapter`

Source: `components/northstar-agent-interop/process_adapter.py`

Bounded local process adapter for CLI-style Agent backends.

#### `ProcessAgentAdapter`

Run a CLI-style backend behind the Northstar handoff contract.

- `execute(handoff_token: str, *, context_ref: str, handoff_secret: bytes, current_policy_revision: str, now: int)`
### `process_backend`

Source: `components/northstar-agent-interop/process_backend.py`

Versioned configuration for process-backed Agent adapters.

#### `ProcessBackendSpec`

- `from_dict(value: Any)`
- `to_dict()`
- `command()`
#### `codex_cli_spec(executable: str)`

#### `claude_code_cli_spec(executable: str)`

#### `cursor_cli_spec(executable: str)`

#### `build_process_adapter(spec: ProcessBackendSpec, *, workspace_resolver: Callable[[str], str | Path], context_loader: Callable[[str], str], timeout_seconds: float=60, max_output_bytes: int=1000000)`

### `remote_worker`

Source: `components/northstar-agent-interop/remote_worker.py`

Remote/hosted-worker readiness scorecard (P3-3) — evidence-based, runnable.

#### `score()`

Per-area (met, total, unmet explanations).

#### `total_score()`

### `ssh_forward`

Source: `components/northstar-agent-interop/ssh_forward.py`

Bounded SSH local-socket forwarding for the Profile A worker channel.

#### `SSHForwardConfig`

Immutable inputs for one SSH local Unix-socket forward.

- `command()`
  - Return the argv that will be passed to ``Popen(shell=False)``.
#### `build_ssh_command(config: SSHForwardConfig)`

Build a non-shell SSH argv with the channel's safety defaults.

#### `SSHForwardError`

An expected channel failure with a contract-level status.

#### `SSHForward`

Start and supervise one bounded SSH local-socket forward.

- `command()`
- `pid()`
- `ready()`
- `stderr()`
- `start()`
  - Spawn SSH and wait until the local socket accepts a connection.
- `check()`
  - Raise if the forward exited or its local socket is no longer usable.
- `stop()`
  - Stop the SSH process group and clean up only this instance's socket.
- `wait_for_exit(timeout_s: float | None=None)`
  - Wait for SSH to exit; a live forward times out rather than hanging.
