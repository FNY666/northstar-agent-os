# northstar-durable-run - API reference

<!-- GENERATED by tests/docbuild.py - do not edit; run `python3 tests/docbuild.py build` -->

Docstring-generated reference over the component's public modules (``ast``-extracted; nothing is imported, so this builds offline with the standard library). The README is the quick start and integration guide:

- [README (quick start)](../../components/northstar-durable-run/README.md)
- [Concepts](../../docs/concepts/governance.md) and [guides](../../docs/guides/governed-run-cookbook.md)

## Modules

### `durable_contract`

Source: `components/northstar-durable-run/durable_contract.py`

Strict schemas for a durable Northstar run's local event boundary.

#### `RunContract`

- `from_dict(value: Any)`
- `to_dict()`
#### `StepContract`

- `from_dict(value: Any)`
- `to_dict()`
#### `EventContract`

- `from_dict(value: Any)`
- `to_dict()`
#### `validate_deadline(run: RunContract, *, now: int)`

#### `can_transition(current: Any, target: Any)`

#### `assert_transition(current: Any, target: Any)`

#### `assert_step_identity(run: RunContract, step: StepContract)`

Assert that a step belongs to the supplied run lineage.

#### `assert_event_identity(run: RunContract, step: StepContract, event: EventContract)`

Assert that an event belongs to both its run and step lineage.

### `cli`

Source: `components/northstar-durable-run/cli.py`

Local inspection and control CLI for one durable-run event stream.

#### `main(argv: Sequence[str] | None=None)`

Run the local durable-run CLI and return a process-style exit code.

### `control_receipt`

Source: `components/northstar-durable-run/control_receipt.py`

Versioned receipts for local durable-run control operations.

#### `digest_state(value: Any)`

Return the canonical digest used to bind a receipt to derived state.

#### `validate_identity(value: Any, field: str='identity')`

Validate and return an identity usable in a control receipt.

#### `ControlReceipt`

Causal summary of one local lifecycle control projection.

- `from_dict(value: Any)`
- `to_dict()`
- `canonical_json()`
  - Return deterministic receipt bytes for an outer signer or store.
- `verify_state(state: Any)`
  - Return whether a replayed state matches this receipt's digest.
### `event_store`

Source: `components/northstar-durable-run/event_store.py`

Append-only local event history and checkpoint support for durable runs.

#### `EventStore`

Persist one durable run's validated, append-only event history.

- `append_event(event: EventContract)`
- `read_history(run_id: str)`
- `derive_state(run_id: str)`
- `replay(run_id: str)`
- `replay_at(run_id: str, sequence: int)`
  - Replay the authoritative event prefix ending at ``sequence``.
- `create_checkpoint(run_id: str)`
- `restore(run_id: str, *, checkpoint: dict[str, Any] | None=None)`
### `action_gateway`

Source: `components/northstar-durable-run/action_gateway.py`

Per-call authorization and approval gateway for the durable-run slice.

#### `sign_approval(approval: dict[str, Any], secret: bytes)`

#### `digest_arguments(arguments: Any)`

#### `ToolCall`

- `from_dict(value: Any)`
- `to_dict()`
- `canonical_json()`
#### `ToolSpec`

#### `ToolExecutionResult`

#### `ActionGateway`

Authorize and dispatch one exact registered tool call at a time.

- `register(spec: ToolSpec)`
- `execute(call: ToolCall, arguments: dict[str, Any], *, authorization_token: str, authorization_secret: bytes, now: int, approval_token: str | None=None, current_policy_revision: str, run: dict[str, Any] | None=None)`
### `runner`

Source: `components/northstar-durable-run/runner.py`

A small local durable runner for the Northstar vertical slice.

#### `StepPlan`

- `input_digest()`
#### `LeaseManager`

A single-owner, expiring local lease persisted as strict JSON.

- `acquire(owner_id: str, *, now: int, ttl_seconds: int)`
- `assert_valid(owner_id: str, *, now: int)`
- `heartbeat(owner_id: str, *, now: int, ttl_seconds: int)`
- `release(owner_id: str)`
#### `DurableRunner`

Execute planned local steps with durable event and lease boundaries.

- `prepare(*, owner_id: str, now: int)`
- `pause(*, owner_id: str, now: int, reason: str='operator pause')`
  - Pause at a durable run boundary; actions already in a Python call are not interrupted.
- `resume(*, owner_id: str, now: int)`
  - Resume a paused run's durable state; execution still reacquires the lease.
- `cancel(*, owner_id: str, now: int)`
  - Persist active step cancellation before the terminal run cancellation event.
- `retry(plans: list[StepPlan], *, owner_id: str, now: int, finalize: bool=True)`
  - Retry a failed run with explicit plans and fresh step idempotency keys.
- `execute(plans: list[StepPlan], *, owner_id: str, now: int, finalize: bool=True)`
  - Execute steps, reusing an unfinished attempt after process recovery.
### `verifier`

Source: `components/northstar-durable-run/verifier.py`

Independent postcondition verification for the durable-run slice.

#### `VerificationResult`

#### `verify_run_completion(run: RunContract, store: EventStore, *, workspace: str | Path, required_files: dict[str, str], test_exit_code: int | None, now: int, claimed_status: str | None=None)`

Verify real local postconditions; never trust a model status claim.

#### `make_final_receipt(run: RunContract, result: VerificationResult)`

### `trace_metrics`

Source: `components/northstar-durable-run/trace_metrics.py`

Small structured trace and metrics boundary for durable runs.

#### `TraceSpan`

- `from_dict(value: Any)`
- `duration_ms()`
- `to_dict()`
- `canonical_json()`
#### `TraceRecorder`

- `record(value: dict[str, Any] | TraceSpan)`
- `spans()`
#### `MetricsSummary`

- `from_spans(spans: tuple[TraceSpan, ...] | list[TraceSpan])`
- `to_dict()`
### `evaluation`

Source: `components/northstar-durable-run/evaluation.py`

Deterministic local task-level evaluation for the durable-run slice.

#### `BenchmarkSummary`

- `to_dict()`
#### `run_fixture_benchmark(root: str | Path, *, case_count: int, fail_case: int | None=None)`

### `durable_audit`

Source: `components/northstar-durable-run/durable_audit.py`

Bridge EventStore events into the canonical NDJSON audit feed (audit v1).

#### `event_to_audit(event: dict[str, Any], *, occurred_at_is_ms: bool=False)`

Map one EventContract dict to a canonical audit record.

#### `events_to_ndjson(events: Iterable[dict[str, Any]])`

Export many events as one canonical NDJSON audit feed text.

#### `iter_events_audit(events: Iterable[dict[str, Any]])`

Lazily map many events into validated audit records.
