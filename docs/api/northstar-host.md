# northstar-host - API reference

<!-- GENERATED by tests/docbuild.py - do not edit; run `python3 tests/docbuild.py build` -->

Docstring-generated reference over the component's public modules (``ast``-extracted; nothing is imported, so this builds offline with the standard library). The README is the quick start and integration guide:

- [README (quick start)](../../components/northstar-host/README.md)
- [Concepts](../../docs/concepts/governance.md) and [guides](../../docs/guides/governed-run-cookbook.md)

## Modules

### `authorization`

Source: `components/northstar-host/authorization.py`

Host-side policy authorization for Northstar runs.

#### `HostPolicy`

An immutable explicit actor-to-capability allowlist.

- `from_mapping(revision: str, actor_capabilities: Mapping[str, Iterable[str]])`
#### `AuthorizationValidation`

#### `sign_authorization(authorization: dict[str, Any], secret: bytes)`

Sign a structurally valid host authorization grant.

#### `verify_authorization(token: str, secret: bytes, *, now: int)`

Verify an authorization grant without returning claims on failure.

#### `authorize_run(run: dict[str, Any], binding: BindingValidation, policy: HostPolicy, *, now: int, secret: bytes, grant_ttl_seconds: int=300)`

Authorize one structurally valid run under an explicit host policy.

### `workspace`

Source: `components/northstar-host/workspace.py`

Host-side opaque workspace allocation for authorized Northstar runs.

#### `WorkspaceAllocation`

#### `WorkspaceBroker`

Re-verify host grants and allocate one opaque private run directory.

- `allocate(run: dict[str, Any], binding_token: str, authorization_token: str, *, current_policy_revision: str, now: int)`
