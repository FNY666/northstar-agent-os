# synthetic_only=true; production_verified=false
# Offline synthetic fixture provenance for S9 only.
# No network, service, credential, SDK, shared/P0, incident, or prior research inputs were used.

- `fixtures/cases.json`: locally authored deterministic cases C01-C14.
- `harness.py`: locally authored chain/signature/replay checker using Python SHA-256 and canonical JSON.
- `outputs/results.json`: generated by `python3 harness.py run` from the fixture set.

Evidence classification policy:
- `confirmed`: directly established by this deterministic fixture execution.
- `inferred`: interpretation from fixture structure, not external evidence.
- `unverified`: the fixture does not establish the property.
- `conflicting`: chain, ordering, signature, authorization, or replay evidence conflicts.
- `inaccessible`: required synthetic validator material is outside the known fixture key set.

Synthetic signature note: the harness uses a keyed SHA-256 construction labelled `synthetic-key`; it is not a production cryptographic signature and provides no key custody, authorization, or trust-root claim.
