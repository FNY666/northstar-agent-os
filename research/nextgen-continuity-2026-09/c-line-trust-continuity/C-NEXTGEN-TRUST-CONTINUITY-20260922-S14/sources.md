# Sources and evidence boundary — S14

This slice is intentionally **offline synthetic evidence only**. No network, real service, SDK, credential, prior study artifact, or public source was accessed. Therefore there are no public sources to cite and no claim is `confirmed`.

- Evidence source: `fixtures/cases.json`, locally authored deterministic fixtures; source type: synthetic fixture; not public evidence.
- Inference engine: `harness.py`, `S14-local-deterministic-v1`; deterministic rule execution, not a production implementation.
- Output provenance: `outputs/results.json` records input SHA-256, raw/unique event counts, deduplication, transitions, reasons, blockers, and explicit limitations.

This file is not evidence of real durability, remote state, exactly-once delivery, rollback, or production readiness.
