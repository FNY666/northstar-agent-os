# S19 sources

本切片全程本地合成，不使用网络、外部资料、真实审计/日志服务、SDK、凭据或既有研究产物。

- `fixtures/cases.json` — 本地生成的 24 个确定性多链 fixtures；非外部来源。
- `harness.py` — 本地 SHA-256 canonicalization 与三态判定实现；非外部来源。
- `outputs/results.json` — harness 的本地派生结果；非外部来源。

所有结论均为 `inferred`，不可视为生产验证或真实审计证据。