# synthetic_only=true
# production_verified=false
# explicit_flags=synthetic_only=true; production_verified=false

S10 source register (offline synthetic only).

- S10-FIXTURE: `fixtures/cases.json`, authored deterministic event traces covering crash-before-write, partial and torn bytes, response-before-record, record-before-response, restart replay, duplicate, loss, conflict, inaccessible segment.
  Evidence status: synthetic fixture definition; not an observation of a production system.
- S10-HARNESS: `harness.py`, local parser/replay model using SHA-256-truncated checksums and deterministic event order.
  Evidence status: confirmed as executable local behavior after the recorded run; not a durability guarantee.
- S10-RESULTS: `outputs/results.json`, generated by the harness.
  Evidence status: confirmed as generated local output; interpretation remains synthetic.

No external sources were consulted because the scope explicitly forbids network and real services. There are therefore no external citations. Inaccessible items are represented only as synthetic fixture state, not as access to any real inaccessible system.

Boundaries: these sources cannot establish real crash durability, remote state, exactly-once, rollback, immutable audit, or production readiness.
