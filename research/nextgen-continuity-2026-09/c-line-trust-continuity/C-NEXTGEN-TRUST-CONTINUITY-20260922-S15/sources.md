# S15 evidence sources

This slice is **offline-only**. There are no external sources, URLs, real services, SDKs, credentials, or production observations. All evidence is generated locally by `fixtures/cases.json`; `sources` in the research manifest are intentionally empty. The harness itself is the provenance-producing instrument, and every result identifies its fixture input hash and synthetic provenance records.

Source policy: synthetic fixtures only; `synthetic_only=true`; `production_verified=false`.
