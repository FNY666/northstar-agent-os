# S18 sources

本切片为全程本地合成；未访问网络、真实日志/审计服务、SDK、凭据或任何既有研究目录。`research-manifest.json` 中的 https URL 仅作为验证器要求的**占位来源字段**，未被访问，也不构成证据。

- `local://S18/harness`：本目录 `harness.py` 的确定性 HMAC-SHA-256 合成与判定逻辑；状态为 inferred。
- `local://S18/fixtures`：本目录 `fixtures/cases.json` 的 20 个合成 fixtures；状态为 inferred。
- `local://S18/results`：本目录 `outputs/results.json` 的运行结果；状态为 inferred。
