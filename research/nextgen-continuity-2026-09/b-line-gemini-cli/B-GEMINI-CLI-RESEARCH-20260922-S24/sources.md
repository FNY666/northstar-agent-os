# S24 sources and evidence classification

`synthetic_only=true`; `production_verified=false`.

- `fixtures/cases.json` — **confirmed (synthetic harness input)**: 14 deterministic single-worker/single-op_id fixtures authored for this slice. It is not an external source and does not represent Gemini CLI behavior.
- `harness.py` — **confirmed (synthetic algorithm)**: executable local rule model. It intentionally models attempt-local records and exact replay; it does not inspect or invoke Gemini CLI.
- `outputs/results.json` — **confirmed (execution output)**: generated by the harness in this directory; reproducible from the two files above.
- Gemini CLI implementation, runtime, remote service, persistence layer, crash/restart behavior, and production telemetry — **inaccessible** in this slice by scope and by design.
- Claims about real Gemini CLI crash durability, remote state, exactly-once delivery, rollback, or production safety — **unverified**, not established by these fixtures.
- The chosen boundary rule (“attempt=1 UNKNOWN remains UNKNOWN locally; attempt=2 is a new local record”) — **inferred** from the synthetic contract, not a claim about an external product.
- Any conclusion that an external system must behave this way — **conflicting** with the absence of product evidence; no external behavior is asserted.

All evidence in this slice is synthetic-only. No prior research directories, shared artifacts, incident directories, staging/canonical materials, service, credentials, or real Gemini CLI were read or used.

- `harness.py` — **confirmed (synthetic algorithm)**: executable local rule model. It intentionally models attempt-local records and exact replay; it does not inspect or invoke Gemini CLI.
- `outputs/results.json` — **confirmed (execution output)**: generated by the harness in this directory; reproducible from the two files above.
- Gemini CLI implementation, runtime, remote service, persistence layer, crash/restart behavior, and production telemetry — **inaccessible** in this slice by scope and by design.
- Claims about real Gemini CLI crash durability, remote state, exactly-once delivery, rollback, or production safety — **unverified**, not established by these fixtures.
- The chosen boundary rule (“attempt=1 UNKNOWN remains UNKNOWN locally; attempt=2 is a new local record”) — **inferred** from the synthetic contract, not a claim about an external product.
- Any conclusion that an external system must behave this way — **conflicting** with the absence of product evidence; no external behavior is asserted.

All evidence in this slice is synthetic-only. No prior research directories, shared artifacts, incident directories, staging/canonical materials, service, credentials, or real Gemini CLI were read or used.
