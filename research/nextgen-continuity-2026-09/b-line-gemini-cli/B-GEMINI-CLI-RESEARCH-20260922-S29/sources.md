# Sources

No external sources were consulted. This deliverable is offline and synthetic-only. All classifications are inferred from the deterministic local harness rule and fixture data; they are not confirmed observations of Gemini, a real CLI, or any external service.

- S29-local-model — inferred — local SHA-256 payload-fingerprint comparison in `harness.py`; synthetic_only=true; production_verified=false.
