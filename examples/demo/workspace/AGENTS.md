# Demo workspace conventions

This repository is the offline demo workspace. The governed loop appends this
file to its system prompt when a run starts here, so the agent sees project
conventions without anyone repeating them per run.
