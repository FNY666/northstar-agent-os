# Demo workspace memory

- Prefer reading `notes.txt` before answering.
- Keep answers to one short sentence in this offline demo.
- This file is **workspace-scoped** only (not a global MEMORY store).
