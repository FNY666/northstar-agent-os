# Optional extra: real OpenTelemetry export, and the SDK the test suite uses to
# prove attributes written after span.end() are discarded (they are, silently, so
# the runtime records usage before it ends every span).
#
# Tracing tests that need this are guarded with skipUnless, so a host without it
# still gets a green run.
opentelemetry-sdk>=1.20
