# Runtime dependencies for components/northstar-agent-runtime.
#
# Both are lazy imports: every other module in this component is importable with
# nothing installed, so the offline test suite (and the scripted provider) runs on a
# bare interpreter. Continuous integration uses Python 3.12.
anthropic>=1.0
opentelemetry-api>=1.20
